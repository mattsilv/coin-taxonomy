[project]
name = "coin-taxonomy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.25.0",
    "tiktoken>=0.9.0",
]

[project.scripts]
serve-site = "python:http.server"
